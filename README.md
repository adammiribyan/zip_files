# Zip Files for OpenLSP

## Install
1. Check this short guide in order to install Redis: http://redis.io/download.
2. `bundle install` to install Sidekiq.

## Start
1. Execute `redis-server` in your console.
2. Run `bundle exec sidekiq` in another tab.
3. `bundle exec rails s`.