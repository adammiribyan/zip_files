<%= form_for(@project, html: { multipart: true }) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :source_work_file %><br />
    <%= file_field_tag :source_work_file %>
  </div>
  <div class="field">
    <%= f.label :delivery_file %><br />
    <%= file_field_tag :delivery_file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
